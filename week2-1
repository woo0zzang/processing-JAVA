//Basic shapes

//Stroke and fill
//; is statement terminator.

size(200,200); // Set the size of the window
smooth(); // anti aliasing
background(255); // Draw a white background 
stroke(0); 
fill(150); // noFill() leaves the shape with only an outline.
ellipse(50,50,100,100);
